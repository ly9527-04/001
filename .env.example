# ── Neon Postgres（Vercel 部署用）──
# 在 Vercel Storage 中创建 Neon 数据库后自动获得此值
DATABASE_URL=postgres://user:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require

# ── JWT 密钥（可选，生产环境建议修改）──
JWT_SECRET=anon-qa-box-secret-2024
